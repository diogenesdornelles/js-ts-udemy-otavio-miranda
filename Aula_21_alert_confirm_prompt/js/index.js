// objeto window
// objeto console

window.alert('Nossa mensagem');
const confirma = window.confirm('confirma?');
const nome = window.prompt('Digite seu nome:');
const idade = parseInt(window.prompt('Digite sua idade:'));

//exercicio

const num1 = parseFloat(window.prompt('Digite um número:'));
const num2 = parseFloat(window.prompt('Digite um número:'));
window.alert(`O resultado é ${num1 + num2}`);
